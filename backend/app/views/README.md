# Revel Views Directory

Coach Connect is currently an API-only Revel service. This directory exists because Revel's development watcher expects `app/views` even when the application does not render server-side templates.
