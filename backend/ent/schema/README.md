# Ent Schemas

Ent schemas will be introduced with the first persisted feature. Each schema change requires an application/repository test and a reviewed Atlas migration under `backend/supabase/migrations`.
