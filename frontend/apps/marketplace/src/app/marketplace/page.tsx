import { ZoneShell } from "@coach-connect/ui";

export default function MarketplacePage() {
  return (
    <ZoneShell
      eyebrow="Marketplace zone"
      title="Football expertise, ready to move."
      description="Listings, seller onboarding, orders, and verified reviews will live behind this independently deployable route boundary."
    />
  );
}
