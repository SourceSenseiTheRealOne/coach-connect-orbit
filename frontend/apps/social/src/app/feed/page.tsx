import { ZoneShell } from "@coach-connect/ui";

export default function FeedPage() {
  return (
    <ZoneShell
      eyebrow="Social zone"
      title="Football conversations start here."
      description="Feed, profiles, discovery, connections, messages, and notifications will be implemented in this independently deployable zone."
    />
  );
}
